package com.bravo.johny.jsonserver.stepDefinitions;


import java.util.List;

/*public class BackgroundSteps extends BaseSteps {

    //UserLoginResponse userLoginResponse;

    public BackgroundSteps(ScenarioContext scenarioContext) {
        super(scenarioContext);
        commentService = new CommentService("");
    }

    *//*@When("I call the authentication endpoint with following details")
    public void iCallTheAuthenticationEndpointWithFollowingDetails(List<CommentEntity> userEntityList) {
        userLoginResponse = userService.login(userEntityList.get(0).getEmail(), userEntityList.get(0).getPassword());
    }

    @Then("I should get the authentication token")
    public void iShouldGetTheAuthenticationToken() {
        String token = userLoginResponse.getData().getToken();
        scenarioContext.setContext("token", token);
    }*//*
}*/
